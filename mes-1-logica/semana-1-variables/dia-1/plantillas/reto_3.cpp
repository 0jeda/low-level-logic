/*
 * Reto 3: Conversión con Pérdida
 * Lee un número de 64 bits (long long) e imprímelo convertido explícitamente
 * (mediante cast) a un int (32 bits) y a un short (16 bits).
 * 
 * Formato de salida:
 * INT=<valor>
 * SHORT=<valor>
 */

#include <iostream>

int main() {
    long long A;
    if (!(std::cin >> A)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Realiza los casts explícitos e imprime los resultados en el formato indicado.
    // -----------------------------------------------------------------

    return 0;
}
