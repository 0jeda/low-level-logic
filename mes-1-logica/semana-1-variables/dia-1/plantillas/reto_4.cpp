/*
 * Reto 4: Multiplicación de Precisión
 * Lee dos enteros con signo de 32 bits (A y B) e imprime su multiplicación exacta
 * en un tipo de dato de 64 bits (long long) sin que ocurra desbordamiento durante la operación.
 * 
 * Formato de salida:
 * PRODUCTO=<resultado_64_bits>
 */

#include <iostream>

int main() {
    int A, B;
    if (!(std::cin >> A >> B)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Realiza la multiplicación en 64 bits usando casting y guárdalo en un long long.
    // -----------------------------------------------------------------

    return 0;
}
