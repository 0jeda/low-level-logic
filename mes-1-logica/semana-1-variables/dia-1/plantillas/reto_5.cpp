/*
 * Reto 5: Préstamo y Vuelta en Unsigned (Underflow)
 * Lee dos enteros sin signo de 32 bits (A y B) e imprime el resultado de A - B.
 * 
 * Formato de salida:
 * RESULTADO=<valor_sin_signo>
 */

#include <iostream>

int main() {
    unsigned int A, B;
    if (!(std::cin >> A >> B)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Realiza la resta A - B e imprime el resultado en el formato indicado.
    // -----------------------------------------------------------------

    return 0;
}
