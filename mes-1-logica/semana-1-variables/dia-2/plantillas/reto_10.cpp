/*
 * Reto 10: Acumulación de Error de Precisión
 * Lee un valor float X. Súmalo a sí mismo 1,000,000 (un millón) de veces
 * en un acumulador float y en un acumulador double.
 * Muestra el resultado de ambos acumuladores.
 * 
 * Formato de salida:
 * FLOAT=<valor>
 * DOUBLE=<valor>
 */

#include <iostream>

int main() {
    float X;
    if (!(std::cin >> X)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Crea un acumulador float initialized a 0.0f y súmale X un millón de veces.
    // Crea un acumulador double initialized a 0.0 y súmale X un millón de veces.
    // Imprime ambos acumuladores en el formato indicado.
    // -----------------------------------------------------------------

    return 0;
}
