/*
 * Reto 7: Truncamiento, Redondeo y Piso a Mano
 * Lee un número decimal double X y calcula su truncamiento, redondeo
 * al entero más cercano, y piso (floor) sin usar <cmath>.
 * 
 * Formato de salida:
 * TRUNC=<valor>
 * REDONDEO=<valor>
 * PISO=<valor>
 */

#include <iostream>

int main() {
    double X;
    if (!(std::cin >> X)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Realiza los cálculos de truncamiento, redondeo y piso de forma manual
    // usando cast a int y condicionales if-else.
    // -----------------------------------------------------------------

    return 0;
}
