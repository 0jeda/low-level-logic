/*
 * Reto 8: Conversión de Temperatura (Fahrenheit a Celsius)
 * Lee un valor double F y realiza la conversión a Celsius.
 * 
 * Formato de salida:
 * C=<valor>
 */

#include <iostream>

int main() {
    double F;
    if (!(std::cin >> F)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Conviértelo usando punto flotante y muestra la salida esperada.
    // -----------------------------------------------------------------

    return 0;
}
