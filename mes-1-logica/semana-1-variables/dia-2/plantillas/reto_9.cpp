/*
 * Reto 9: Control de División por Cero Flotante
 * Lee dos double A y B. Realiza la división A / B.
 * Si B es cero, detecta si el resultado es INF, -INF o NAN y muéstralo.
 * 
 * Formato de salida:
 * RESULTADO=<INF | -INF | NAN | valor_decimal>
 */

#include <iostream>

int main() {
    double A, B;
    if (!(std::cin >> A >> B)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Si B es 0, determina el resultado matemático correcto e imprímelo.
    // De lo contrario, imprime el resultado ordinario de la división.
    // -----------------------------------------------------------------

    return 0;
}
