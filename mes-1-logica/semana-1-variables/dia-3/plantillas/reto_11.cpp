/*
 * Reto 11: Desplazamiento ASCII Cíclico (Cifrado César)
 * Lee un carácter C y un desplazamiento entero K.
 * Desplaza C de manera circular en el rango ASCII [32, 126] (95 caracteres).
 * 
 * Formato de salida:
 * CIFRADO=<carácter>
 */

#include <iostream>

int main() {
    char C;
    int K;
    // std::noskipws evita que cin ignore los espacios en blanco
    std::cin >> std::noskipws >> C >> K;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Aplica el desplazamiento K de manera circular en el rango [32, 126]
    // -----------------------------------------------------------------

    return 0;
}
