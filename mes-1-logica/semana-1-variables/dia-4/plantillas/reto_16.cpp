/*
 * Reto 16: Par o Impar Binario
 * Lee un entero N y determina si es PAR o IMPAR usando & 1.
 * 
 * RESTRICCIÓN:
 * No uses el operador módulo %.
 */

#include <iostream>

int main() {
    int N;
    if (!(std::cin >> N)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Determina si N es par o impar usando operaciones de bits.
    // -----------------------------------------------------------------

    return 0;
}
