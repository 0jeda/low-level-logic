/*
 * Reto 18: Apagar Bit en la Posición P
 * Lee un entero N y una posición de bit P (0 a 31).
 * Pon en 0 el bit en la posición P de N y muestra el resultado.
 * 
 * Formato de salida:
 * RESULTADO=<valor>
 */

#include <iostream>

int main() {
    int N, P;
    if (!(std::cin >> N >> P)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Apaga el bit P y guárdalo en una variable o en N.
    // -----------------------------------------------------------------

    return 0;
}
