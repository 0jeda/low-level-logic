/*
 * Reto 19: Encender Bit en la Posición P
 * Lee un entero N y una posición de bit P (0 a 31).
 * Pon en 1 el bit en la posición P de N y muestra el resultado.
 * 
 * Formato de salida:
 * RESULTADO=<valor>
 */

#include <iostream>

int main() {
    int N, P;
    if (!(std::cin >> N >> P)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Enciente el bit P e imprime el resultado.
    // -----------------------------------------------------------------

    return 0;
}
