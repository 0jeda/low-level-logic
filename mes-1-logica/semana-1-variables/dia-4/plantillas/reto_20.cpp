/*
 * Reto 20: El Secreto del Complemento a Dos
 * Lee un entero N, obtén su negación a nivel de bits (~N) y luego súmale 1.
 * Muestra ambos valores por consola.
 * 
 * Formato de salida:
 * NOT=<valor_not>
 * NEG=<valor_neg>
 */

#include <iostream>

int main() {
    int N;
    if (!(std::cin >> N)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Calcula la negación de bits e imprime el resultado.
    // Suma 1 a esa negación e imprime el resultado.
    // -----------------------------------------------------------------

    return 0;
}
