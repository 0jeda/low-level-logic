/*
 * Reto 21: Aritmética de Desplazamientos
 * Lee un entero con signo N y un exponente K >= 0.
 * Multiplica N por 2^K y divide N entre 2^K usando desplazamientos.
 * 
 * RESTRICCIÓN:
 * No uses los operadores * ni / para multiplicar o dividir.
 * 
 * Formato de salida:
 * MULT=<valor>
 * DIV=<valor>
 */

#include <iostream>

int main() {
    int N, K;
    if (!(std::cin >> N >> K)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Realiza los cálculos usando << y >> e imprime en el formato.
    // -----------------------------------------------------------------

    return 0;
}
