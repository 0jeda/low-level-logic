/*
 * Reto 22: Extractor de Bit P
 * Lee un entero N y una posición P.
 * Determina si el bit en la posición P está encendido (1) o apagado (0).
 * 
 * Formato de salida:
 * BIT=<0 | 1>
 */

#include <iostream>

int main() {
    int N, P;
    if (!(std::cin >> N >> P)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Extrae el bit P e imprime BIT=<valor>
    // -----------------------------------------------------------------

    return 0;
}
