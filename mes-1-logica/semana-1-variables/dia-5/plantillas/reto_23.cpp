/*
 * Reto 23: Máscara de Rango Completo
 * Lee dos índices L y R (0 <= L <= R <= 31).
 * Crea una máscara unsigned int de 32 bits con unos en los bits L a R.
 * 
 * RESTRICCIÓN:
 * No utilices bucles. Resuélvelo en tiempo constante O(1).
 * 
 * Formato de salida:
 * MASCARA=<valor_decimal>
 */

#include <iostream>

int main() {
    int L, R;
    if (!(std::cin >> L >> R)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Construye la máscara de rango usando operaciones de bits e imprímela.
    // -----------------------------------------------------------------

    return 0;
}
