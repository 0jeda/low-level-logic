/*
 * Reto 24: Cuenta de Bits Encendidos en un Byte (Bucle)
 * Lee un byte B (entero 0 a 255) y cuenta sus bits encendidos en 1
 * usando un bucle.
 * 
 * Formato de salida:
 * CANTIDAD=<valor>
 */

#include <iostream>

int main() {
    unsigned int B;
    if (!(std::cin >> B)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Recorre los bits de B usando un bucle y cuenta cuántos son 1.
    // -----------------------------------------------------------------

    return 0;
}
