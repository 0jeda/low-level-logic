/*
 * Reto 25: Intercambiador de Nibbles (Nibble Swap)
 * Lee un byte B (entero 0 a 255).
 * Intercambia los 4 bits más significativos con los 4 bits menos significativos.
 * 
 * Formato de salida:
 * NUEVO=<valor>
 */

#include <iostream>

int main() {
    unsigned int B;
    if (!(std::cin >> B)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Intercambia los nibbles de B usando máscaras y desplazamientos de bits.
    // -----------------------------------------------------------------

    return 0;
}
