/*
 * Reto 28: Rotación Circular de Bits (Shift Circular)
 * Lee un unsigned int N y una cantidad de rotación R (0 <= R <= 31).
 * Rota los bits de N circularmente a la izquierda en R posiciones.
 * 
 * Formato de salida:
 * ROTADO=<valor_decimal>
 */

#include <iostream>

int main() {
    unsigned int N;
    int R;
    if (!(std::cin >> N >> R)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Rota N circularmente a la izquierda R posiciones de forma segura.
    // -----------------------------------------------------------------

    return 0;
}
