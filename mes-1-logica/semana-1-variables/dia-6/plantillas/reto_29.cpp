/*
 * Reto 29: Palíndromo Binario en un Byte
 * Lee un byte B (entero 0 a 255) y determina si su representación
 * binaria de 8 bits es simétrica.
 * 
 * Formato de salida:
 * PALINDROMO=<ES_PALINDROMO | NO_ES_PALINDROMO>
 */

#include <iostream>

int main() {
    unsigned int B;
    if (!(std::cin >> B)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Verifica si la representación de 8 bits de B es un palíndromo.
    // -----------------------------------------------------------------

    return 0;
}
