/*
 * Reto 30: Sumar 1 sin usar Aritmética Directa (Ripple Carry)
 * Lee un entero N y súmale 1.
 * 
 * RESTRICCIÓN:
 * Está prohibido usar los operadores +, -, ++, o --.
 * Debes resolverlo únicamente con operadores de bits y bucles.
 * 
 * Formato de salida:
 * RESULTADO=<valor>
 */

#include <iostream>

int main() {
    int N;
    if (!(std::cin >> N)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Suma 1 a N sin usar operadores aritméticos de suma/resta directos.
    // -----------------------------------------------------------------

    return 0;
}
