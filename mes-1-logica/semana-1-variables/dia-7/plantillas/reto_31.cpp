/*
 * Reto 31: Contar Bits Encendidos (El Reto Semanal)
 * Lee un entero N y cuenta cuántos bits están en 1 en su representación binaria.
 * 
 * RESTRICCIÓN:
 * Usa solo operaciones de bits y un bucle.
 * 
 * Formato de salida:
 * BITS=<cantidad>
 */

#include <iostream>

int main() {
    int N;
    if (!(std::cin >> N)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Cuenta los bits encendidos en N usando desplazamientos y máscaras.
    // -----------------------------------------------------------------

    return 0;
}
