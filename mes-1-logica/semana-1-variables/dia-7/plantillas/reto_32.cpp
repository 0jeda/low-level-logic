/*
 * Reto 32: Potencia de Dos en Tiempo Constante O(1)
 * Lee un entero N y determina si es una potencia de 2.
 * 
 * RESTRICCIÓN:
 * Está prohibido usar bucles o recursión. Resuélvelo en O(1).
 * 
 * Formato de salida:
 * POTENCIA=<SI | NO>
 */

#include <iostream>

int main() {
    int N;
    if (!(std::cin >> N)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Comprueba en O(1) si N es potencia de 2.
    // Pistas: Recuerda validar si N <= 0.
    // -----------------------------------------------------------------

    return 0;
}
