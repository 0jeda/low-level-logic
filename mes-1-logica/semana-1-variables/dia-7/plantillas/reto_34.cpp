/*
 * Reto 34: Inversión Completa de Bits (Bit Reversal)
 * Lee un unsigned int N e invierte el orden de sus 32 bits.
 * 
 * Formato de salida:
 * REVERSADO=<valor_decimal>
 */

#include <iostream>

int main() {
    unsigned int N;
    if (!(std::cin >> N)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Reversa el orden de los 32 bits de N e imprime el resultado.
    // -----------------------------------------------------------------

    return 0;
}
