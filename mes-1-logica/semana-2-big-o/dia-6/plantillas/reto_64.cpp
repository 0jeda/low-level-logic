/*
 * Reto 64: Detector de Anagramas
 * Lee dos palabras en letras minúsculas (longitud <= 100).
 * Determina si son anagramas usando un arreglo de frecuencias de tamaño 26.
 * 
 * Formato de salida:
 * ANAGRAMA=SI  (si son anagramas)
 * ANAGRAMA=NO  (de lo contrario)
 */

#include <iostream>
#include <string>

int main() {
    std::string S1, S2;
    if (!(std::cin >> S1 >> S2)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Verifica si S1 y S2 son anagramas usando conteo de caracteres.
    // -----------------------------------------------------------------

    return 0;
}
