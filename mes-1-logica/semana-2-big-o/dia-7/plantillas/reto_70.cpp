/*
 * Reto 70: Peor Caso de la Búsqueda Binaria
 * Lee N (1 <= N <= 2 * 10^9).
 * Calcula el número máximo de comparaciones de la búsqueda binaria para N en O(log N).
 * 
 * Formato de salida:
 * COMPARACIONES=<cantidad>
 */

#include <iostream>

int main() {
    long long N;
    if (!(std::cin >> N)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Calcula la cantidad de divisiones en el peor caso.
    // -----------------------------------------------------------------

    return 0;
}
