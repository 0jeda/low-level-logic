/*
 * Reto 89: Ordenamiento de Cadena de Caracteres
 * Lee una palabra (string S, longitud <= 100).
 * Ordena las letras de la palabra alfabéticamente usando Insertion Sort.
 * 
 * Formato de salida:
 * ORDENADO=<palabra_ordenada>
 */

#include <iostream>
#include <string>

int main() {
    std::string S;
    if (!(std::cin >> S)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Ordena las letras de S usando Insertion Sort.
    // -----------------------------------------------------------------

    return 0;
}
