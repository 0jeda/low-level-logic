/*
 * Reto 103: Rellenar Matriz en Serpiente
 * Lee R y C (1 <= R, C <= 50).
 * Rellena la matriz de 1 a R x C en forma de serpiente (filas alternas).
 * 
 * Formato de salida:
 * SERPIENTE=
 * <elementos fila 0 separados por espacio>
 * ...
 */

#include <iostream>

int main() {
    int R, C;
    if (!(std::cin >> R >> C)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Crea, rellena e imprime la matriz en forma de serpiente.
    // -----------------------------------------------------------------

    return 0;
}
