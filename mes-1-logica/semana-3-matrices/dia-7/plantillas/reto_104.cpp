/*
 * Reto 104: Rellenar Matriz en Caracol (Espiral)
 * Lee N (1 <= N <= 15).
 * Rellena una matriz N x N con valores de 1 a N^2 en espiral.
 * 
 * Formato de salida:
 * CARACOL=
 * <fila_0>
 * ...
 */

#include <iostream>

int main() {
    int N;
    if (!(std::cin >> N)) return 1;

    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ
    // Rellena la matriz en caracol/espiral e imprímela.
    // -----------------------------------------------------------------

    return 0;
}
