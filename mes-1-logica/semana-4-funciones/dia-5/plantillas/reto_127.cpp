/*
 * Reto 127: Factorial Recursivo Puro
 * Lee N.
 * Llama a factorial(N) e imprime el resultado.
 *
 * Formato de salida:
 * FACTORIAL=<resultado>
 */

#include <iostream>

long long factorial(int n) {
    // -----------------------------------------------------------------
    // TU CÓDIGO AQUÍ: Implementa recursivamente el factorial.
    // -----------------------------------------------------------------
}

int main() {
    int N;
    if (!(std::cin >> N)) return 1;

    std::cout << "FACTORIAL=" << factorial(N) << "\n";

    return 0;
}
